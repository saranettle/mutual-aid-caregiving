<html>
<!--
 Original UI design
 credit Prof. Danielle Safonte
 bsg_html_UI.zip
 https://canvas.oregonstate.edu/courses/1967354/assignments/9690212?module_item_id=24460842
-->
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Visits</title>
        <link rel="stylesheet" href="./static/css/main.css">
        
    </head>

    <body>
    <header>
        <h1>Mutual Aid Caregiving</h1>
        <h2>Visits</h2>
        <nav>
            <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/neighbors">Neighbors</a></li>
            <li><a href="/locations">Locations</a></li>
            <li><a href="/communities">Communities</a></li>
            <li><a href="/visits">Visits</a></li>
            <li><a href="/certifications">Certifications</a></li>
            <li><a href="/visit_types">Visit Types</a></li>
            <li><a href="/certify_neighbors">Certify Neighbors</a></li>
            <li><a href="/community_neighbors">Community Neighbors</a></li>
            <li><a href="/about">About</a></li>
            </ul>
        </nav>
    </header>

    <main>
            <p>On this page, you can view all of the visits, edit the visits, or delete them. Note that these visits are not filtered by community. Also note that when creating a visit, you do not need to add a caregiver immediately, since this may be fulfilled by another neighbor.</p>

            <p>To add a visit, please return to the <a href="/">home page</a>, and select the community that you want to create the visit for.</p>
            
            <table>
            <!-- Access data tuple passed from app.py --> 
            {% if data %}
			    <thead>
				    <tr>
					    <!-- Iterate through each key in the first entry to get the column name -->
                        {% for key in data[0].keys() %}
					    
                        <!-- Create a <th> tag with the key inside of it, this will be our header row -->
					    <th scope="col" data-field="{{ key }}" data-filter-control="input">{{ key }}</th>
					
                        <!-- End of this for loop -->
                        {% endfor %} 
                        <th scope="col" data-filter-control="input"></th>
                        <th scope="col" data-filter-control="input"></th>
                    </tr>

				<tbody>
					<!-- Now, iterate through every item in data -->{% for item in data %}
					    <tr>
						    <!-- Then iterate through every key in the current item dictionary -->
                            {% for key in item.keys() %}

						    <!-- Create a <td> element with the value of that key in it -->
						    <td> {{item[key]}}</td> 
                            {% endfor %}

                            <!-- Create our edit/delete buttons -->
						    <td><a href="edit_visit/{{item.visitID}}" class="btn btn-default">Edit</a></td>
						    <td><a href="delete_visit/{{item.visitID}}" class="btn btn-default">Delete</a></td>

					    </tr> 
                        {% endfor %} 
                {% endif %} 
            </table>   
 
    </main>

    <footer>
        <p>Made by Chris Blum and Sara Nettle 2024</p>
    </footer>

    </body>
</html>